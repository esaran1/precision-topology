.venv/
__pycache__/
.pytest_cache/
.matplotlib/
*.pyc
*.csv.tmp
*.lock

# 2.7 GB raw per-step training log — over GitHub limits.
# Regenerate with src/phase2b_ordering.py; derived results are committed.
results/phase2b_checkpoints.csv

# built supplementary archives (rebuild from the tag, never commit)
dist/

# deterministic lag-test-2 checkpoints (regenerate: python -m src.lag_test2 checkpoints)
results/lag_test2_states/
