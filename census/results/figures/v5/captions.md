# Figure captions (v5): inputs for the LaTeX captions

Generated by `python -m src.figures_v5` from the committed artifacts. Figures are 5.5 in wide, at most 3 in tall, every glyph at least 8 pt (Times). Colours: blue = conditional minimisation (thresholds and predictions built on it); vermillion = free training's placement from an unplaced start; green = stays placed / solves; grey = context.

## setting

- **PDF**: `results/figures/v5/setting.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: For a > 1 the activation f_a folds, and only a folded unit can put both outer windows on the same side of the inner window: a trained non-monotone network solves the task and the best monotone one cannot.
- **Population and n**: (a) f_a(t) = t + a sin t for a = 0.9, 1.05 and 2.0 (closed form). (b) Inner window [−0.8, 0.8] (class 0, negative) and outer windows ±[1.2, 2.0] (class 1, positive); two trained width-1 networks recovered deterministically from seeds (float64, Adam lr 0.01, 2,000 steps): a = 1.5, seed 0 (solves; dense 4,001-point check) and a = 1.0, seed 38 (the best monotone run, 91/200 sample errors). Outputs shown through tanh.
- **Uncertainty shown**: None (a closed form and two individual runs).
- **Numbers for the caption** (moved out of the image):
  - fold depth at a = 2: D(2) = 2(√(a²−1) − arccos(1/a)) = 1.3697 (the double arrow)
  - the folded unit's sign pattern across the windows is (+, −, +); the monotone unit changes sign only once across them
- **Note**: Replaces paper/figures/fig1_setting.pdf (v4 styling).

## metric_check

- **PDF**: `results/figures/v5/metric_check.pdf` (PNG preview beside it)
- **Placement**: main text (or appendix)
- **Message**: The continuous test error falls over a wide range of output scale while the binary solve rate is still zero, so the sharp threshold is not an artifact of a binary metric.
- **Population and n**: Width 1, 2,400 training runs (fold1d sweep and refinement; certified Ĝ), binned by R in 0.025 bins. (a) Fraction of runs that solve. (b) Test errors per run: mean (squares) and interquartile band.
- **Uncertainty shown**: Interquartile band of the per-run test errors in (b); none in (a).
- **Numbers for the caption** (moved out of the image):
  - binary solve rate: 10–90% transition over R ∈ [0.330, 0.429] (bar in (a))
  - continuous error: 10–90% of its fall over R ∈ [0.055, 0.342] (bar in (b)); width ratio 2.9
  - 76% of the error improvement occurs in bins where no run solves (1,815 runs, R < 0.30)
- **Note**: Replaces paper/figures/fig4_metric_check.pdf (v4 styling, twin axis).

## decomposition_float32

- **PDF**: `results/figures/v5/decomposition_float32.pdf` (PNG preview beside it)
- **Placement**: main text (replaces decomposition if the main text uses the float32 population; WP-26)
- **Message**: Training fails by not placing the hidden unit; below ε ≈ 0.25 every run fails this way, and bias failures appear only in a narrow band around ε ≈ 0.4.
- **Population and n**: Width 1, the single-draw float32 population: 2,400 runs, 200 per a at 12 values of a (Adam lr 0.01, 2,000 steps).
- **Uncertainty shown**: Clopper–Pearson 95% intervals per a.
- **Numbers for the caption** (moved out of the image):
  - totals: 430 solved, 1,664 placement failures, 306 bias failures
  - the same runs as the width-1 sweep and refinement (and the metric check)
- **Note**: Final round: same design as 'decomposition', restricted to the float32 draw.

## lag

- **PDF**: `results/figures/v5/lag.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: The crossing lag grows linearly with the ratio of output growth to branch relaxation, with a slope κ(a) computed from the landscape with no fitted parameter (derived after a fitted relationship was known).
- **Population and n**: Width 1, all 36 free-training arms and tests (the four intervention experiments, SGD, and the fresh-sample test at a = 1.45 and 1.60); each point is an arm's median lag against its median ratio. The lag is measured from the switch of the branch each run tracks, on its own training sample (math note §13; WP-31). Filled markers: Adam; open markers: SGD. Marker shape: a = 1.30 circles, 1.45 squares, 1.50 triangles, 1.60 diamond. Grey crosses: the exact linear response along each run's own trajectory, started at 0.7 s* (arm medians; post hoc, predictions committed before the comparison).
- **Uncertainty shown**: None drawn (arm medians); lines and crosses are predictions, not fits.
- **Numbers for the caption** (moved out of the image):
  - κ(1.30) = 7.61, κ(1.45) = 4.59, κ(1.50) = 4.05, κ(1.60) = 3.29 (lines r = κ(a)χ)
  - measured from each sample's global own threshold instead (Track 1A, registered tolerance: 36 of 36 arms within), the observed/predicted through-origin slope per a is 0.89 (1.30), 0.83 (1.45), 0.91 (1.50), 0.82 (1.60)
  - exact linear response, observed/predicted lag per arm (ratio of arm medians over the runs with a prediction, 1,708 of 1,750 runs; tracked-branch reference): 1.00–1.05
- **Note**: New figure (final round).

## scoreboard

- **PDF**: `results/figures/v5/scoreboard.pdf` (PNG preview beside it)
- **Placement**: main text or appendix
- **Message**: Every registered prospective width-1 prediction that states a value or a range, observed against predicted: the timescale and threshold predictions hold, and the tests outside the sine family and in R² fail.
- **Population and n**: (a) Lag predictions: fresh-sample timescale test (circles, a = 1.45, 1.60), SGD extension (squares), learning-rate invariance (upward triangles; η = 0.005 and 0.0025 against η = 0.01), GELU/SiLU/Mish (downward triangles, 200 seeds), single unit in R² (plus signs). (b) Threshold-error predictions: own-sample threshold (circles), early branch (squares) and lag-corrected early branch (triangles), held-out windows (diamonds, 8 settings, spread horizontally for visibility). Filled: within the registered tolerance or range; open: outside it.
- **Uncertainty shown**: Grey bars: the registered tolerance (prediction ± tolerance) or registered range.
- **Numbers for the caption** (moved out of the image):
  - Task B: 2 of 2 within
  - SGD: 2 of 2 within
  - learning rate: 4 of 4 within
  - other activations: 0 of 3 within
  - R^2: 0 of 2 within
  - own-sample threshold: 2 of 2 within
  - early branch: 2 of 2 within
  - early branch, lag-corrected: 2 of 2 within
  - held-out windows: 7 of 8 within
  - Not plotted (no stated predicted value; registered as paired comparisons, all PASS): own-threshold vs population (Task B, SGD, own-seed P1), own-seed P2a/P2b/P3, held-out windows C vs B1/B2/B3
  - Not plotted (not a point prediction): the forced ramp (slopes and rank correlations; WP-24)
- **Note**: New figure (final round).

## mechanism_w1

- **PDF**: `results/figures/v5/mechanism_w1.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: As output scale grows, the conditional minimiser's first-layer weight moves from the class-mean optimum α* toward the worst-case optimum, dropping below the placement bound a/1.4 before placement switches on at R_glob, where free training crosses.
- **Population and n**: Width 1, a = 1.30 and 1.50, 800-point population. Minimiser: the conditional audit's retained minimiser (s ≥ 1.5) and the same frozen search at s = 0.05–1.25 (a validated search, not a certificate). R_glob: the certified bracket midpoint. Crossings: phase 2b, budget 32,000, 38 and 38 crossing runs.
- **Uncertainty shown**: None (point values; the bracket for R_glob is narrower than the line).
- **Numbers for the caption** (moved out of the image):
  - α* = 1.7913 (the class-mean optimum, independent of a); a/1.4 = 0.929 (a = 1.30), 1.071 (a = 1.50)
  - |w1| at the smallest scale shown: 1.751 (a = 1.30), 1.756 (a = 1.50)
  - the minimiser drops below a/1.4 at R/R_glob ≈ 0.54 (a = 1.30) and 0.76 (a = 1.50): the bound is necessary for placement, not sufficient
  - crossings with |w1| < a/1.4: 100% and 100%
  - w2_glob = 4.9563 (a = 1.30), 2.5312 (a = 1.50)
- **Note**: Do not describe the minimiser path as certified; only R_glob is.

## fixed_scale

- **PDF**: `results/figures/v5/fixed_scale.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: Held at a fixed output scale, an unplaced network becomes placed, and a placed one stays placed, only above the threshold from conditional minimisation; both transitions sit just above it.
- **Population and n**: a = 1.30. Becomes placed: 543 pre-placement checkpoints per held level, read after 4,000 steps. Stays placed: 177 runs per level, read after 12,000 steps. Held levels R/R_glob = 0.6, 0.8, 0.9, 1, 1.1, 1.25, 1.5, 2; R_glob is the certified threshold at a = 1.30 (midpoint of its bracket).
- **Uncertainty shown**: Clopper–Pearson 95% intervals on each fraction.
- **Numbers for the caption** (moved out of the image):
  - n = 543 checkpoints per level (becomes placed); n = 177 runs per level (stays placed)
  - midpoints (50% crossing of the fraction): 1.049 (becomes placed), 1.067 (stays placed); shown rounded to 1.05 and 1.07 on the plot
  - the two curves are read at different horizons: 4,000 steps (becomes placed) and 12,000 steps (stays placed)
  - Adam's moments are preserved from the checkpoint in both curves (the reset variant is in the appendix figure)

## app_fixed_scale

- **PDF**: `results/figures/v5/app_fixed_scale.pdf` (PNG preview beside it)
- **Placement**: appendix (companion to fixed_scale)
- **Message**: Resetting the optimiser's moments and running 4× or 16× longer leave both transitions where they are.
- **Population and n**: As fixed_scale; (c): 543 replays per level at held R/R_glob = 0.90–1.10, moments preserved.
- **Uncertainty shown**: Clopper–Pearson 95% intervals.
- **Numbers for the caption** (moved out of the image):
  - (a) midpoints 1.049 (moments kept) and 1.049 (reset)
  - (b) midpoints 1.067 (kept) and 1.068 (reset)
  - (b) earlier run: held at 1.15× an earlier estimate of R_glob = 1.156 R_glob; 33/37 stayed placed against a criterion of at least 0.9
  - (c) midpoints 1.0458 (4k steps), 1.0450 (16k steps), 1.0454 (64k steps)
  - (c) n = 543 replays per level

## decomposition

- **PDF**: `results/figures/v5/decomposition.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: Training fails by not placing the hidden unit: below ε ≈ 0.25 every run fails this way, and bias failures appear only in a narrow band around ε ≈ 0.4.
- **Population and n**: 12 values of a (ε = 0.02–2); n = 400 runs per a (200 seeds × 2 precisions); 4,800 runs in total.
- **Uncertainty shown**: Clopper–Pearson 95% intervals on each fraction.
- **Numbers for the caption** (moved out of the image):
  - n = 400 runs per a; 4,800 runs in total
  - outcome counts: failed to place (G ≤ 0) 3,297; placed but output bias outside its interval 633; solved 870
  - 0 disagreements between this classification and the independent solve check

## thresholds

- **PDF**: `results/figures/v5/thresholds.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: At every a, free training places the hidden unit just above the conditional-minimisation threshold R_glob and far below R_solve; both thresholds have certified limits as ε → 0.
- **Population and n**: a = 1.30–1.60 (six values); free-training crossings at budget 32,000: n = 38 runs per a.
- **Uncertainty shown**: Violins: the distribution of crossing R; points: medians with bootstrap 95% intervals. The certified threshold brackets are narrower than the markers (plotted at their midpoints).
- **Numbers for the caption** (moved out of the image):
  - n = 38 crossing runs per a (budget 32,000)
  - certified R_glob brackets: widths ≤ 1.4e-03; R_solve brackets: widths ≤ 1.4e-03
  - ε → 0 limits: R_glob^∞ ∈ [0.1985926, 0.1985927] (sharp value); R_solve^∞ ∈ [0.3067, 0.3071] (global)
  - R_glob^∞ rests on K = sup G₀, certified by branch and bound over a box with a domain lemma (math note §8); its independent Arb check (verify_certificates.py, python-flint/Arb) verifies it (WP-17)

## app_thresholds_small_eps

- **PDF**: `results/figures/v5/app_thresholds_small_eps.pdf` (PNG preview beside it)
- **Placement**: appendix (companion to thresholds)
- **Message**: At small ε the certified thresholds follow the certified first-order line R_glob^∞(1 + c₁ε).
- **Population and n**: 4 certified brackets at a = 1.01–1.04 and the limit; no training runs.
- **Uncertainty shown**: Boxes are the certified brackets, to scale.
- **Numbers for the caption** (moved out of the image):
  - c₁ ∈ [0.2852300, 0.2852303]; the first-order range is certified for |ε| ≤ 0.05
  - R_glob^∞: sharp value [0.1985926, 0.1985927] (line) inside the unconditional bracket [0.19738, 0.19920] (grey box)
  - the registered c₁ test was INCONCLUSIVE: feasible c₁ ∈ [0.225, 0.330], width 0.105 > 0.1

## minimiser_gap

- **PDF**: `results/figures/v5/minimiser_gap.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: Along the conditional minimiser at a = 1.30, the class gap changes sign exactly once, at R_glob.
- **Population and n**: a = 1.30, population objective (800 points); the conditional minimiser at 39 output scales (line) and the certified global minimiser at 20 scales (squares).
- **Uncertainty shown**: None drawn: the certified values are exact to ≤ 1e-09 in loss.
- **Numbers for the caption** (moved out of the image):
  - x = R/R_glob = |w₂| / 4.95625 (midpoint of the certified R_glob bracket in |w₂|)
  - certified brackets: R_glob at |w₂| ∈ (4.9500, 4.9625]; R_solve at |w₂| ∈ (7.1000, 7.1125] (R_solve/R_glob = 1.434)
  - the minimisers either side of the switch are at most 2.7e-04 apart (certified); other basins are at least +0.0031 above (certified, radius 0.1)

## app_candidates

- **PDF**: `results/figures/v5/app_candidates.pdf` (PNG preview beside it)
- **Placement**: appendix (companion to minimiser_gap)
- **Message**: No candidate of the conditional search lies below the retained minimiser at any scale.
- **Population and n**: a = 1.30: 2,701 candidates at 59 evaluations.
- **Uncertainty shown**: None.
- **Numbers for the caption** (moved out of the image):
  - 1,194 discarded as degenerate; 976 screening runs; 413 full runs that were not the lowest (they lie on the retained minimiser)
  - frozen search result reproduced at 59/59 evaluations; 20 certified global minima; 10 stricter-optimisation points

## mirror_branches

- **PDF**: `results/figures/v5/mirror_branches.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: A run crosses just above the own threshold of the mirror branch it occupies at the crossing, not that of the branch its initialisation selects.
- **Population and n**: Free-training crossings at budget 32,000, a = 1.30 and 1.50, n = 38 runs each; thresholds in R units (R = |w₂|Ĝ/2).
- **Uncertainty shown**: None drawn; the caption gives Spearman ρ with bootstrap 95% intervals.
- **Numbers for the caption** (moved out of the image):
  - a = 1.30: ρ = 0.997 [0.9872, 0.9996] (n = 38); initialisation-selected branch ρ = 0.13; own global threshold ρ = 0.88; median residual log(R/T) = 0.030
  - a = 1.50: ρ = 0.995 [0.9801, 0.9983] (n = 38); initialisation-selected branch ρ = 0.12; own global threshold ρ = 0.88; median residual log(R/T) = 0.063
  - a = 1.30: 20 runs on branch +, 18 on branch −
  - a = 1.50: 20 runs on branch +, 18 on branch −
  - the caption must say this analysis is POST HOC (exploratory); the label is no longer in the image

## prospective

- **PDF**: `results/figures/v5/prospective.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: In eight held-out settings, the conditional threshold with one fitted lag factor predicts the median crossing better than either baseline.
- **Population and n**: 8 held-out settings (4 window geometries × 2 a), 90 runs each; n = 86–88 crossings per setting.
- **Uncertainty shown**: Horizontal bars: bootstrap 95% intervals of the observed median crossing R.
- **Numbers for the caption** (moved out of the image):
  - n = 86–88 crossings of 90 per setting
  - lag factor fitted on the base window: λ(1.30) = 1.115, λ(1.50) = 1.164
  - baselines: the base window's median crossing |w₂| (triangles); the pooled median crossing R of earlier windows, 0.2280 (diamonds)
  - placement was checked every 50 steps; the detection-sensitivity analysis (WP-6: crossings interpolated between checks, calibration and observations both) keeps every registered comparison's sign, each excluding 0

## prospective_own

- **PDF**: `results/figures/v5/prospective_own.pdf` (PNG preview beside it)
- **Placement**: main text
- **Message**: A run's own threshold, fixed before training, predicts its crossing better than the population threshold, with or without a fitted factor.
- **Population and n**: 16 never-trained settings (8 windows × 2 a), 60 runs each; n = 460 of 480 crossed at a = 1.30 and 460 of 480 at a = 1.50.
- **Uncertainty shown**: (b): window-level bootstrap 95% intervals (10,000 resamples, seed 0) of the mean per-run |log error|.
- **Numbers for the caption** (moved out of the image):
  - (a) the own thresholds' frozen brackets are at most 8.4e-04 wide (no longer drawn)
  - (a) per-setting Spearman ρ between crossing and own threshold: 0.76–0.86 (a = 1.30), 0.57–0.82 (a = 1.50)
  - (b) the four predictors: population = R_glob; population, fitted = λ(a)·R_glob (λ fitted on the base window); own = R_own (the run's own threshold); own, fitted = ρ_res(a)·R_own
  - (b) fitted ρ_res (C_own): 1.0311 (a = 1.30), 1.0660 (a = 1.50)
  - (b) the registered criteria (P1, P2a, P3, P4 at a = 1.30; P1, P2b, P3, P4 at a = 1.50) all passed
  - placement was checked every 50 steps; the detection-sensitivity analysis (WP-6) finds every registered criterion passes with interpolated crossings too, but at a = 1.50 the reading 'C beats U_own' does not survive (−0.003 [−0.013, 0.008])

