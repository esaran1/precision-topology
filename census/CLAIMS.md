# Claims ledger

One row per claim. **Status**: current / qualified / withdrawn / superseded.
This file is the authority on counts; where a results document disagrees, a
correction block there points here. Withdrawn claims stay listed — the
correction history is part of the record.

Statistic kinds: *count* (exact-zero counts, the strongest kind here),
*extreme* (minima/maxima, quoted only with distributions), *mean±SD* (seed
level), *measurement* (direct computation on a specific object).

## Topology / monotonicity strand

| # | Claim | Status | Evidence (n) | Kind | Known threats | Where |
|---|---|---|---|---|---|---|
| T1 | **Monotonic width-3 zero**: no monotonic-activation width-3 network has reached 0 eval errors under SGD | **current** | 0 separations in **5,580 distinct** runs (width 240, threshold 1,280, parametrization 1,440, corrugation 1,620 distinct — 270 bit-identical Reading-B duplicates excluded, see T23 — protocol 360, restarts **400**, winding 240) spanning five link families; verified programmatically from artifacts. *Audit correction 2026-08-24: previously stated 5,540 with a restarts stratum of 360; `search_restarts.csv` holds 400 monotonic rows (200 tanh + 200 sin a=0.95), all at 0. The zero is unchanged; the n was understated.* | count | eval is 2,000 points (but regional ⇒ sample-level, so the zero is conservative); all runs share Adam/Kaiming ancestry except protocol stratum | `threshold_results.md`, `search_results.md`, `reading_b_anomaly.md`, `monotonic_zero.md` (decomposition), `AUDIT.md` |
| T2 | **Power bound on T1**: the monotonic width-3 separation rate is below **0.054%** (one-sided exact 95%, n = 5,580 distinct; exact bound 0.0537%), below 3.7% for any single 80-run cell | current | (1−p)^5580 = 0.05 | count-derived | the pool is a heterogeneous mixture of links/depths/protocols; the bound applies to that mixture, not to each condition. *Audit correction 2026-08-24: at the previously stated n = 5,540 the exact bound is 0.0541%, so "below 0.054%" was false by rounding; at the corrected n = 5,580 it is 0.0537% and the wording holds.* | `monotonic_zero.md` (authoritative statement), `threshold_results.md`, `AUDIT.md` |
| T3 | **Non-SGD searches also find no monotonic separation**: swap-descent (24×2), annealing (12 traces, all fail before reaching a = 1; first-failure midpoints span [1.075, 2.275], see T19), CMA-ES (0/120 restarts, positive control 12/20 at d3) | current | as listed, each family separate — never pooled with T1 | count | CMA objective changed after positive-control failure (documented); annealing path-dependence | `search_results.md` |
| T4 | **Monotonic networks can shatter the sample**: 0 *train* errors on 2,000 points (3 CMA restarts), 2 *eval* errors by SGD — while T1 stands | current | 3 train-0 CMA candidates; 8 SGD runs < 9 errors | measurement | — | `search_results.md`, `threshold_results.md` |
| T5 | **The transition offset is reachability, not expressivity.** Refined dense-verified intervals: A (1.08, 1.09], B (−0.20, −0.25]. The theory's possibility direction (strict local extremum, no margin condition) asserts existence for every parameter past threshold, an exhibit realizes it at `a = 1.02` (T25), four searches fail there, no matched activation scalar aligns the two families (all candidates disjoint after bisection, including min-derivative), and training health is smooth through the onset. The transition marks where SGD finds the map, not where it exists. | current; supersedes the earlier necessary-but-not-sufficient framing | bisection 720 runs, dense-verified; A4 searches; A1 theory reading; A3/A5 analyses | count + exhibit | onset rates below CI resolution under `a = 1.08` (0/40 ceilings 8.8%); grid resolution | `offset_results.md`, `threshold_theory.md` |
| T6 | **Monotonic networks can shatter the sample and cannot separate the region.** The barrier is at exactly 0 eval errors, approached arbitrarily closely (2 errors by SGD, 0 *train* errors by CMA-ES) without being reached. The floor argument (populated 9–15 band, hard edge) is **retracted** as an SGD-search artifact of the three fixed activations. | current; floor argument retracted | sin-family runs at 2/6/8 errors; 3 CMA train-0 candidates; 0 at exactly zero (T1) | count + extreme (with distributions) | — | retraction blocks in `corrugation_results.md`, `parametrization_sensitivity.md`; `threshold_results.md` P3 |
| T7 | **GELU/non-monotonic width-3 separations exist and are regional**: witness at 0 errors on 2,000,000 points (margin 0.28); 35 GELU dense survivors | current | dense checks at 100k/run; witness 2M | measurement | dense sampling is still sampling; margins down to 0.017 in survivors | `witness.md`, `dense_check.md` |
| T8 | **Sample-level separation overstates regional separation**: 56 of 163 width-3 separations (46/81 GELU) fail at 100k points | current | 163 reconstructed runs, 163/163 recovery checks passed | count | flip-prone band near 1-in-100k margins (documented) | `dense_check.md` |
| T9 | Corrugated-link GELU separation counts (34 at width 3; "18 of 21 configurations") | **qualified twice**: sample-level only, and duplicate-contaminated — distinct counts are **28 separations across 18 distinct configurations (15 with separations); 5 survive densely; 14 of 18 distinct configurations have zero dense-verified separations** | dense check + dedup | count | see T8, T23 | `corrugation_results.md` + notes, `dense_check.md`, `reading_b_anomaly.md` |
| T10 | **Fold layer 1 is the overwhelming regularity, and it is rare-not-forced**: layer 1 in every dense-verified trace except `generic` d12 s7, which folds at layer 2 and is itself dense-verified — a **confirmed existence proof of later folding**, not a possible artifact. Corrugation 5/5 at layer 1, parametrization 17/18, baseline 2 (+1 marginal), witness by construction. The strong corrugation negative ("fold stays at 1 at 18× embeddedness, frequency 200") is **restated**: dense-verified support covers only mild corrugation; the strong-corrugation gap is now **closed**: all 11 dense-verified width-3 runs from the corrugation_width sweep — including at published amplitude 0.3 and frequency 200 — fold at layer 1 (`corrugation_closure.md`) | dense-verified traces as listed; witness trace lk −1→0 at fold, residual 2e−15 | measurement | 512-point traces under-resolve deep distorted nets (fixed: 8,192 + convergence gate); corrugated trace resolution not re-audited | `dense_check.md` addendum, `corrugation_results.md`, `witness.md`, `localization_results.md` |
| T11 | **Monotonic layers never change \|lk\|**; sign flips exactly track negative-determinant layers; near-miss runs crush inter-component distance to 10⁻³–10⁻⁴ instead | current | 8 monotonic traces, every converged measurement; det-sign match 100% | measurement | unreportable gaps where curves crush below artifact threshold | `localization_results.md` |
| T12 | **Layer-1 fold is irreplaceable**: monotonic students for layer 1 converge from three distinct initializations to a common optimum at chance (1023 errors; seed propagation verified, finals agree to 1.5e−8 — a replicated optimum, not three scattered runs); a **full-depth** monotonic student reaches ~88 errors in a minority of seeds (**2 of 5**: {88, 89, 1000, 1000, 1000}; seeds 2–4 sit at a second common optimum at chance). The contrast is an existence statement: full-depth monotonic approximation of the teacher's function is sometimes trainable sample-level; replacing layer 1 in place never is. *Audit correction 2026-08-25: previously quoted "88 errors" was the best of three with the chance seed unmentioned; extension seeds 3–4 (localization_distill_extension.csv) show chance is the majority outcome.* | current | layer 1: 3 seeds, one optimum; full depth: 5 seeds | measurement + extreme (with distribution) | single teacher network | `localization_results.md`, `AUDIT.md` |
| T13 | Error geography: near-miss errors sit toward the other component (8/8 runs); strong angular clustering only in some | current, partial | 8 runs | measurement | small error counts per run | `localization_results.md` |
| T14 | Width-4 practical sufficiency for all activations | qualified: every activation separates *sometimes* at width 4, but ReLU-family rates ≤6% and densely weaker still (ReLU 1/5, leaky 3/10 sampled) | sweeps + 200-run dense sample | count | dense sample is a sample | `width_sweep.md`, `dense_check.md` |
| T15 | Tail-vs-bulk: GELU advantage confined to the tail; corrugation narrowed bulk gap 12→4 while monotonic floor rose 9→16 | qualified: bulk medians and the tail *direction* stand; the floor-movement component (9→16 as barrier signature) is **retracted with the floor argument** (T6); corrugated tail counts are sample-level (T9) | 5,040-run corrugation sweep | count + medians | see T6, T9 | `corrugation_results.md` + retraction |
| T16 | **Projected linking uniform-zero on width >3 traces** | **withdrawn** | — | — | overturned by the cancellation control: distorted Hopf embeddings project to a clean integer 0 in 17–23% of random projections; original fidelity control used rigid rotations only | `linking_projected.md`, `cancellation_control.md` |
| T17 | Corrugation embeddedness boundaries 277× / 200× past published values | **withdrawn**, superseded by **18× / 19×** (amplitude limit 0.01657, frequency limit 5.18) | chord-vs-arc bug; found because the test rejected a plain circle | measurement | — | correction block in `corrugation_readings_prediction.md` |
| T18 | G.1 reading ambiguity is immaterial to every result | current, with a scope note: Reading B as implemented is one fixed 50% modulation (amplitude ignored, T23), so "both readings agree" compares Reading A against that single Reading-B condition family, not against a swept Reading B | both readings: 0 monotonic separations, fold layer 1, minima 23/24 | count | see T23 | `corrugation_results.md`, `reading_b_anomaly.md` |
| T19 | Annealing localization, restated with an explicit definition: taking a\* = midpoint between the last separating and first failing parameter on the descent, **first** failures span a\* ∈ [1.075, 2.275] (median 1.275); 2 of 12 traces are **re-entrant** (fail, recover, fail — depth-5 seeds 10 and 16), and their *final* loss of separation is at 1.375 and 1.125; no trace fails below the analytic threshold a = 1 (min 1.075). *Audit correction 2026-08-25: previously "every trace loses at a\* ∈ [1.1, 1.8]" — an interval matching no definition of the statistic (values exceed it at both ends), with re-entrance unreported.* | current | 12 traces, recomputed in `AUDIT.md` | measurement | path/schedule dependence; a\* definition is descent-order-dependent for re-entrant traces | `search_results.md`, `AUDIT.md` |
| T20 | Estimator validity: Gauss estimator exact on unlink/Hopf/(2,4)-torus/chain; winding links verified lk = −q | current | validation suite + `winding_validation.csv` | measurement | — | `linking_validation.md`, `test_winding.py` |
| T21 | Regional separation collapses above mild corrugation at width 3 | **superseded** — the zero was underpowered, as its own power note warned: 30 fresh seeds at depth 5 found dense-verified width-3 separations at the published amplitude (A_paper 1/30) and at every strong configuration tested (rate ~3%). The surviving statement is a *gradient*, not a threshold: difficulty grows with amplitude at all widths (T26) | corrugation_width sweep, 1,350 runs | count | — | `corrugation_closure.md`, `corrugation_dense_rates.md` |
| T23 | **Reading-B implementation flaw, resolved anomaly**: the Reading-B sampler ignores its amplitude parameter (fixed 50% modulation) — `B_a0.05`/`B_a0.15`/`B_a0.5`/`B_paper` are one condition, bit-identical run-for-run (720 duplicate rows) — and its clip concentrates ~20% of points exactly on the tube surface, making dense verification a surface probe (83% of one failed run's dense errors exactly on the surface; min between-class distance *larger* than flat, 0.70 vs 0.60, ruling out proximity). The Reading-B dense-zero (0/5 distinct) is therefore a sampling-design effect, not evidence about corrugation strength or folds | current | distribution comparison, bit-identity check, error-location check; pinned by `test_corrugation_reading_b.py` | measurement | n = 5 distinct separations; surface-margin account verified on one run in detail | `reading_b_anomaly.md` |
| T24 | **Reachability degrades with linking number while the width bound holds**: dense-verified separation rates, **pooled over the two non-monotonic activations**, fall monotonically with \|lk\| at every width (per-activation curves have small non-monotonic cells: gelu w4 0%→15% at q3→q4, sin w5 95%→100% at q1→q2); the smallest width with a dense-verified strict majority grows 4 → 4 → 5 → 6 over q = 1–4 (sin 2.0; the q = 4 step rests on a knife-edge cell, w5 exactly 10/20); tanh needs one more width at q = 1, two more at q = 2, and reaches no majority at any width ≤ 6 for q ≥ 3; width 4 retains ≥1 dense-verified separation at every q (no tension with Theorem D.1). Non-monotonic width-3 separation reaches zero by q = 3 (0/40 cells, bound 7.2%). Dense attrition is higher at q ≥ 3 than q ≤ 2 (22%, 28%, 46%, 42% — not monotone; previously quoted as "grows 22% → 46%", which selected the q=3 endpoint) | current | 1,600-run winding sweep, geometry validated at lk = −q, dense verification built in | count | q ≥ 3 width-3 zeros are small cells; single depth (5); one strong non-monotonic representative | `winding_results.md` |
| T25 | **Existence exhibit at `a = 1.02`**: a width-3 network whose only non-monotonicity is `f_{1.02}` — used in its 5.3e−3-deep dip via the F.2 recipe (shrink into the quadratic neighbourhood of the local max, amplify 600×), monotone tanh continuation — separates **1,400,000 fresh points at 0 errors** (margins 0.53–0.91), lk −1 → 0 exactly at the `f_{1.02}` layer. Four search families (476 attempts: 400 restarts, 24 fine-tunes, 12 anneals, 40 CMA-ES) find nothing at the same value; SGD's best is 3 errors | current | `src/offset_witness.py` deterministic; pinned by tests; searches in `offset_search.csv` | exhibit + count | continuation is trained (semi-analytic, as with the Part 2a witness); fold-shape pilots documented | `offset_results.md`, `offset_prediction.md` |
| T26 | **Corrugation is generic difficulty, width-amplified, amplitude-driven**: dense-verified rates flat to amplitude 0.3, cliff at 0.5 at every width (w4 → 0/30 both activations; w6 halved-or-worse), frequency nearly irrelevant to 200; monotonic tanh hit at least as hard as GELU at widths 4/6, so no interaction with the topological obstruction; fold layer 1 on all 11 dense-verified strong-corrugation width-3 runs | current | 1,350-run corrugation_width sweep + B1/B2 analyses | count + trend (Spearman +0.47/+0.61) | single depth (5); Reading A + flat only (Reading B vacuous per T23) | `corrugation_closure.md` |
| T27 | **The findability gap is a weight-scale barrier, shown by manipulation**: solutions at `a = 1.02` need ~1,000× fold amplification; standard training reaches ~50 (median) to ~150 (max) there; `A_req` crosses into the trained distribution exactly at the onset (1.05: 253 vs tail 333; 1.09: 126 vs median 44); and initializing layers 1–2 at the construction's scale pattern (random directions, standard Adam) yields **17/40 dense-verified separations at 1.02 against 0/400 standard**. Interpolation barriers fall 1,065 → 10 → 1 across the onset. Exhibit upgraded: pure all-f_a depth-4 networks separate at 1.02 | current | registered predictions 2b-tail and 2b-intervention borne out; v1 intervention failure (0/40, flawed init) recorded | intervention + count | spectral-norm product is a crude amplification proxy; single depth | `gap_results.md`, `amplification_prediction.md` |
| T28 | **Negatives from the gap investigation, all registered**: basin radius does not explain findability (recovery curves flat in `a` — 50% crossings between ε = 0.1 and 1.0 with no trend — while rates vary 2.5–29%; the earlier "ε₅₀ ≈ 0.1–0.3" quoted a range matching no stated estimator); constructed solutions sit in basins ~2 orders narrower than found ones at the same `a` (calibration failed — constructed exhibits are atypical objects); `A_req` is undefined for Family B (shear, not scale, blocks its construction; SGD still finds B solutions); the GELU construction failed on its grid (GELU existence rests on 35 found dense-verified solutions; its gap is a rate gap); basin geometry does not predict SGD success rates across activations | current | basin/amplification/part3 artifacts | count + measurement | — | `gap_results.md` |
| T29 | **The scale intervention reaches the standard setting, two-sided**: GELU width-3 dense-verified rate 3.0% (0.3× init) → 8.0% (standard) → 17.0% (found-separator-pattern init), n = 200/arm, Fisher (one-sided, directions registered) up-vs-standard p = 0.0048, down-vs-standard p = 0.023, up-vs-down p = 1.5e−6; two-sided: 0.0096 / 0.046 / 2.9e−6. Under Bonferroni ×3 the up-vs-standard and up-vs-down comparisons survive; **down-vs-standard does not (0.138)** — the dose-response headline rests on the scaled-up arm, with the down arm directionally consistent at nominal significance only. Registered R2 expectation (no change) was exceeded — endpoint spectral products do not distinguish separators (percentiles 0.34–0.86), so the operative variable is the scale training *traverses early*, set by initialization, not endpoint scale. Scaled init also raises dense attrition (33% vs 11%) | current | three 200-seed arms, registered before the scaled arms ran | count + intervention | single depth/width; found-solution pattern (weaker than a construction pattern) | `gelu_scale_results.md`, `gelu_scale_prediction.md` |
| T30 | **The gap reproduces without topology**: in the 1D width-1 fold task (impossibility provable in one line), monotonic zero exact (0/200 everywhere), family-A onset (1.30, 1.35] **at the standard configuration (Adam, lr 1e-2, 2,000 steps)** — *qualified 2026-08-28: the onset is optimizer- and step-size-specific, not a task property; at Adam lr 3e-2 it lies below a = 1.25 (83/200 there vs 0/200 at baseline), see `step_size_results.md`* — predicted by required-vs-reachable |w₂| (≈1.2/depth vs measured reach ~6), family-B gap **absent** (119/200 = 59.5% at α=−0.05; 57.8% pooled over α<0) exactly as its reachable required scale predicts; solution sets are thin sheets whose **basin volume jumps at the onset while solution measure grows smoothly** — the measure, recomputed analytically after the grid artifact (`grid_measure_audit.md`), is **positive and strictly monotone at every a > 1** (7.6e−7 at 1.05 rising to 1.8e−3 at 3.0, growth factors ×1.8–×9, no discontinuity) while basin fraction is **exactly 0 through a = 1.25** and then 0.4% → 5.2% → 50%. **Solutions exist with no basin at all far below the onset and at ordinary norm** — verified at a = 1.02 with |w₂| = 1, well inside [−5,5]⁴ and inside the scale training reaches — so the gap is not that solutions sit beyond training's reach in norm, but that they sit **where training does reach and have no basin**; **[withdrawn 2026-08-25: solutions exist inside [−5,5]⁴ at every a tested, including a = 1.02 with |w₂| = 1; the reported emptiness was a 41⁴-grid resolution artifact — see `box_emptiness_correction.md`. All solution_fraction values are lower bounds at grid resolution.]**. 2D/3D-nested settings unresolved (3 solves in 2,200 runs, all non-monotonic) | current | registered predictions; exact grid geometry | count + exact measure | onset calibration is architecture-dependent by design; intermediate dimensions open | `fold1d_results.md`, `box_emptiness_correction.md`, `notes/generality_framing.md` |
| T31 | **The perturbation estimator checks out against exact ground truth**: unbiased as a sampler (20-draw vs 300-draw truth within 0.006); measures recovery-including-background (upward bias ≈ from-scratch rate — negligible near threshold where committed numbers live, and correcting it pushes further against the basin-radius account); constructed-vs-found narrowness is regime-dependent (real 3× sheet-thinning at amplification 23, two orders at 600) with fragility concentrated in the fold-position direction; basins strongly anisotropic (radius ≠ volume) | current | ground-truth grids + 300-draw curves + per-coordinate probes | measurement | validated at 1D a=1.5; link-setting extrapolation argued, not measured | `fold1d_results.md` (2c) |
| T32 | **Width dependence of the activation advantage at d = 3** (Task E, in-setting): categorical at width 3 (monotonic 0/300 fresh-seed runs at depth 3, 0/120 at depth 6; GELU 2/100, sin(1.5) 9/100 dense-verified); the monotonicity-specific advantage (vs tanh) is Fisher-n.s. from width 6; vs the ReLU family it persists to width 8 — a **ReLU-family optimization deficit shared by monotonic tanh-vs-ReLU comparisons** (98 vs 84 at width 8, p = 8e−4), not a monotonicity effect — and every pairwise comparison is n.s. from width 12 with all cells 100/100 at widths 16–32; no residual convergence-speed advantage at large width (GELU slightly slower than piecewise-monotonic). Registered P-W1 **failed at width 8** as written (dead units do not rescue it: leaky-ReLU fails 7/100 there without dying); P-W2's shape and P-W3's ceiling borne out; depth-6 slice shows the same shape with no detected interaction (n = 40) | current | 5,300 runs, seeds 100–199 (out-of-sample vs width_sweep), dense-verified primary outcome | count | coarse decay shape partly postdiction re width_sweep.md (declared in registration); single lr/optimizer | `width_effect_results.md`, `width_prediction.md` |
| T33 | **Width-sweep secondary results at d = 3**: additive scaling supported, 5d not (every activation saturated by width 8, three of four by 6; no material change 8 → 15); the width-3/5 accuracy trimodality is **dying ReLU** (chance-run fraction scales with depth, 30% → 90% at width 3 across depths 3–12; near-zero for tanh/GELU), an optimization failure separable from the width-3 ceiling that survives excluding it; dead-ReLU rates moved only ~5 points under the author's protocol (minibatch, lr/10, early stop — registered prediction of substantial reduction **not borne out**, making the figures robust to protocol) | current | 2,880-run width sweep; 1,920-run protocol comparison | count + distribution | sample-level accuracies (dense checks cover a width-3/4 subset); protocol stratum n = 360 monotonic | `width_sweep.md`, `protocol_comparison.md` |
| T34 | **MNIST bottleneck test failed, reversed** (Task E 2a): registered GELU-over-tanh advantage near intrinsic dimension does not exist — tanh beats GELU at *every* bottleneck width 2–48 (two-sided p ≤ 0.009, 10 seeds/cell), with **no width structure** (flat +40–90 errors, indifferent to the ID band [7, 13]); the setting **fails its own positive control** (GELU never beats ReLU either), so the data cannot distinguish "width prediction wrong on real data" from "setting lacks the phenomenon"; every run censored below the 99% train criterion (3-epoch budget), and the tanh lead exists in training error too (short-budget optimization regime) | current | 300 runs; ID measured by TwoNN/MLE k10/k20 on the bottleneck input (spread [7, 13]) | count + registered failure | 3-epoch budget; convergence untested (would be a new registration); single architecture | `bottleneck_results.md`, `bottleneck_prediction.md`, `intrinsic_dimension.csv` |
| T35 | **The folk GELU-over-ReLU advantage: a reproduction map** (Task F): absent in 2-layer and deep MNIST MLPs (depth-8 pilot gap failed n = 10 fresh-seed replication, p = 0.07; depth 12 p = 0.98); **replicated mid-descent in CIFAR-10 small CNNs at two depths** (depth 8: 2,050 vs 2,182, p = 1.2e−4; depth 4: 2,498 vs 2,614, p = 6e−3; n = 10 fresh seeds each) with **tanh strictly worst wherever the advantage exists** (p = 4e−4 vs GELU — the smoothness explanation is contradicted in this setting) and the advantage **erased by 500-step warmup in the pilot** (n = 3, lead not claim); **the pre-registered restoration criterion is now met in full** (2026-08-26): rerunning depth 8 to convergence gives GELU 2,047.0 vs ReLU 2,179.7 (n = 10 each, two-sided permutation **p = 3e−5**) with **24 of 25 runs plateaued**; the 2 censored runs are reported as censored and dropping them leaves p = 6e−5. The 12-epoch ranking was **stable** to convergence (gap 132 → 133). At convergence tanh is **worst of the three** (2,591.8; 545 behind GELU, p = 4e−4; 412 behind ReLU, p = 4e−4) with 4× the training error — so the advantage tracks **non-monotonicity, not smoothness**, and the 1c kill switch stays un-fired. **The positive control is restored**, which unlocks the Part 2 width test; it does *not* establish that the fold mechanism causes the CIFAR advantage | current | criterion fixed before search (`control_criterion.md`); 27-run pilot map + 45-run replication + 25-run convergence run pending | count + registered criterion | one architecture family; no augmentation; single lr; depth 4 tested only at 12 epochs; mechanism untested (Part 2) | `control_restoration.md`, `control_criterion.md`, `task_f_conclusion.md` |
| T36 | **Construction-pipeline negative control**: the F.2 pipeline's precondition (strict local extremum via sign change of f′ with usable slope) fails at step 1 for every monotonic activation for the analytic reason in each case (tanh sech² > 0; ReLU-family f′ ≥ 0; sin(0.98) f′ ≥ 0.02), and finds genuine folds for every non-monotonic one including SiLU unprompted — the boundary of "cannot start" coincides with analytic monotonicity, so exhibits produced by it cannot smuggle in solutions for monotonic activations | current | 10 activations through the pipeline | measurement | the control checks the precondition, not the continuation training | `gap_part4_control.md` |
| T37 | **Lower bound on required amplification (the project's one theorem)**: in the 1D task, any solving `w₂·f(w₁x+b₁)+b₂` with logit margin m satisfies **\|w₂\| ≥ 2m/G(w₁,b₁) ≥ 2m/(κ·D(a))**, where D(a) is f_a's dip depth and κ = G\*/D ∈ [0.305, 0.328] is fixed by the task windows (not the activation); b₂ cancels in the derivation so the bound holds for the optimal b₂, and w₁ cannot substitute for w₂ (it enters only through G ≤ κ·D). Since D(a) ~ (8/3)(a−1)^{3/2}, required scale diverges like (a−1)^{−3/2} as a → 1⁺. **Verified against 66 solvers recorded weeks before the theorem: 0 violations, median slack 1.27×, min 1.04× (tight).** **The infimum of \|w₂\| over solutions is 0, not positive** — margin can vanish, so no bound of this form can explain unfindability; the vanishing-margin solutions are exactly the zero-basin ones (a = 1.02, \|w₂\| = 1, margin 2.7e−4, basin 0), and margin alone does not draw the line (SGD's smallest found margin at a = 1.5 is 0.0168, *below* the \|w₂\|=1 construction's 0.0384). **That seam — necessary condition on solutions vs. sufficient condition for SGD to reach one — is the boundary between this theorem and a theory of findability, and is stated as §5 rather than as a caveat.** Ordering, stated explicitly in §3: **the registered expectation failed** (derived c ≠ measured \|w₂\|·D ≈ 1.2), **then** the theorem was verified against 66 solvers recorded weeks earlier for another purpose (0 violations, min slack 1.04×), **then** the failure was diagnosed — that statistic is margin-dependent and drifts (2.30, 2.32, 4.02, 6.54 at a = 1.45–3.0), so a match would have agreed with an artifact of how it was summarized rather than with the geometry. The failure is the more informative outcome and is recorded as such. | current | 66 re-derived solvers; κ computed on a 33×400 (b₁,w₁) grid | theorem + verification | bounds solutions, not SGD; silent on basins; **does not extend to width > 1** (multiple units can split the two sign changes, leaving only Σᵢ\|w₂ᵢ\|Gᵢ ≥ 2m) | `fold1d_theorem.md`, `src/fold1d_theorem.py` |
| T38 | **The CIFAR GELU advantage is not a smoothness effect** (the standalone result of the restored control): at convergence in the depth-8 CIFAR CNN, **tanh — smooth and monotonic — is worst of the three**, 545 test errors behind GELU (two-sided permutation p = 4e−4) and 412 behind ReLU (p = 4e−4), with **4× the training error** (744 vs ~180 per 10,000), i.e. underfitting rather than generalizing differently. Since tanh is smooth and GELU's distinguishing property is non-monotonicity, **the standard smoothness explanation of GELU's advantage is contradicted in this setting**. Does *not* establish that the fold mechanism causes the advantage — that is the width prediction, untested (T39). | current | n = 10/10/5, all plateaued, permutation tests 100k resamples | count + measurement | one architecture family; no augmentation; single lr; a constraint on explanations, not a demonstration of mechanism | `task_f_conclusion.md`, `control_restoration.md`, `cifar_convergence.csv` |
| T39 | **The width prediction is untested on real data, and the reason is a finding**: testing it needs a setting with *both* (a) a reproducible GELU-over-tanh advantage and (b) a flat bottleneck whose width is unambiguous with a stable ID estimate. Three experiments show no available setting has both — **MNIST bottleneck**: axis defensible (ID spread [7, 13]), control **absent** (GELU never beat ReLU at any width, T34); **MNIST/CIFAR deep MLPs**: axis defensible, control **absent** (depth-8 replication p = 0.07, depth-12 p = 0.98, T35); **CIFAR depth-8 CNN**: control **restored** (all four criteria), axis **not defensible** — conv width is ambiguous (channels 128 / spatial 4 / product 512), and at the one flat location the ID estimators disagree **1.99×** systematically (TwoNN 45–51 vs MLE 26–30, a 1.7× gap in every representation, not noise), implying overlapping test regions. The pattern is structural: the folk advantage reproduces in convolutional settings, which are exactly where "width" stops being one number. Closed by decision rather than run, since a CIFAR-MLP control was ~a coin flip and a *narrow* pass would rest the prediction on a weak control. `task_f_conclusion.md` §3 specifies what a qualifying setting must satisfy, so the test is well posed for anyone who finds one. | current | 3 settings; 300 + 45 + 25 runs plus 8 ID measurements | registered-criterion outcome + measurement | untested, **not refuted**; candidates not tried are listed | `task_f_conclusion.md`, `cifar_width_axis_verdict.md`, `cifar_width_axis.md`, `bottleneck_results.md` |
| T40 | **Findability in the 1D task is set by optimizer reach, not by energy or basin size.** *(Terminology 2026-08-28: the zero-basin objects here are non-critical points, so an absent basin is expected rather than anomalous; the measurements are unaffected — `terminology_correction.md`.)* (i) Barriers: every minimum-energy-path barrier is exactly 0 and the linear proxy anti-correlates with the rate, so the Arrhenius/escape account is **falsified** (the 1,065→10→1 collapse that motivated it measured the constructed endpoint's own amplification, not the landscape). (ii) Step size: the *largest* Adam step is best everywhere — at a = 1.25, lr 3e-2 gives 83/200 against 0/200 at baseline, moving the onset below 1.25 — falsifying the registered "smaller steps thread a thin sheet" direction; plain SGD shows no monotone step trend at all, so the effect is Adam-specific. (iii) Geometry: the solution sheet's half-width is constant in \|w₂\| for w₁/b₁ and grows linearly for b₂, and basin recovery is flat-to-rising in \|w₂\| — **no peak**, falsifying P-4a/4b. (iv) What remains: `reach` (fraction of runs whose final \|w₂\| exceeds a value) collapses 0.65 → 0.21 → 0.00 across \|w₂\| = 3 → 5 → 8, matching where SGD lands, and large-step runs land at \|w₂\| = 9.9–14.9 exactly where standard reach is 0. Isotropic 3× init scaling *reduces* reach and solve rate (38/100 → 7/100), falsifying P-4c's direction — distinct from the per-layer spectral pattern of T27/T29. | current | barrier 13×20 paths; step sweep 2 optimizers × 4 a × 5 lr × 200 seeds + fixed-budget control; manifold 11 \|w₂\| values | count + measurement | one task, one depth; "reach" is budget-dependent by construction | `barrier_results.md`, `step_size_results.md`, `manifold_results.md` |
| T41 | **Correctly-classifying points that GD does not terminate in are excluded from four standard accounts by measurement.** *Corrected 2026-08-28 (`criticality_results.md`, `terminology_correction.md`): these points are **not critical points** (gradient norm 0.02–0.27, i.e. 20–227× training's own terminal gradient norm; loss 0.22–0.69) and **λ_min < 0 at six of eight** values, so (a) "unreachable minima" was wrong — they are non-critical points on a slope — and (b) the claim that Ahn–Zhang–Sra "does not apply" was tested on only one of its two conditions (λ_max > 2/η) and not the other (λ_min < 0). The eigenvalue tests and exclusions below stand as measurements.* (i) **Sharpness**: exact 4×4 Hessians give λ_max·η_eff = **0.025–0.030** for zero-basin solutions and 0.0005–0.0036 for found ones, against the Ahn–Zhang–Sra threshold of **2** — 70× inside the stable regime, so their Theorem 1 does not apply and is not violated; registered P-EoS-1b (separation at 2) **falsified**, as were P-EoS-1c (λ_max *rises* with \|w₂\|: 2.91→5.01) and P-EoS-1d (λ_max → **2.7**, not ∞, as a → 1⁺; non-monotonic with a peak near a = 1.35). (ii) **Barrier**: MEP exactly 0.000 everywhere (T40). (iii) **Distance**: zero-basin solutions sit **4.7–5.5** from typical init versus **10.2–17.7** for found ones — nearer, not further. (iv) **Margin**: at a = 1.5 the zero-basin margin is 0.0384 and **5/20 found solutions have smaller margin**. Mechanism (λ_max bounded): the BCE Hessian carries s(1−s) ≤ ¼ and these solutions have loss 0.56–0.69, so thinness of the solution set and sharpness of the loss are **different quantities**. The derived capture cross-section \|w₂\|·G*(a)/step orders rates across a (Spearman 0.98) but is **falsified under intervention** — flat at 36–40 while the rate moves 0 → 0.5, and largest (1293) where the rate is 0. | current | exact Hessians; MEP 5 paths/cell; 8 a values × 2 populations; intervention 2 a × 4 lr × 40 seeds | measurement + registered failure | four accounts checked, not exhaustiveness; reached-\|w₂\| predicts but is not itself derived from hyperparameters | `sharpness_results.md`, `exclusion_table.md`, `capture_results.md`, `thin_target.md` |
| T22 | **Transition located by regional separations** (robustness): every separation at the interval-defining values passes dense verification while dense failures cluster at `a ≥ 1.5`, away from the boundary — the transition location rests on the strictest-verified runs in the project | current | dense re-derivation | count | — | `threshold_results.md` correction block, `dense_check.md` |

## Precision / quantization strand

| # | Claim | Status | Evidence (n) | Kind | Known threats | Where |
|---|---|---|---|---|---|---|
| P1 | bfloat16 tanh final-layer vector non-injectivity 56.88% ± 4.92% vs 0.35% ± 0.35% at float32 | current | 5 seed-level averages | mean±SD | acceptance gate excludes failed runs | `FINDINGS.md` |
| P2 | Boundedness (not injectivity alone) is the operative property; positive excess only for tanh | qualified: post-hoc factorial, n=1 bounded activation; orthogonal to the ICML monotonicity ordering | 3 activations | mean±SD | untested prediction registered (sigmoid/softsign) | `FINDINGS.md` |
| P3 | Blobs collapse more than linked tori → effect tracks separability, not topology | current | control dataset | mean±SD | — | `FINDINGS.md` |
| P4 | Class-purity reading of collision structure | **withdrawn** (circular: purity was computed on a quantity that presupposed it); replaced by the per-class/linking distinction note | — | — | — | `FINDINGS.md` correction, `notes/icml_paper_reconciliation.md` |
| P5 | `collisions(F)` is empty in float32 (measure-zero); exact-collision counting cannot detect topology change at full precision | current | direct check | measurement | — | `interleaved_quantization.md` |
| P6 | Interleaved vs post-hoc quantization: `collisions(QF) ⊇ collisions(F)` verified; `collisions(G) ⊇ collisions(F)` fails (positive control established after 3 attempts) | current | per-layer comparisons | measurement | — | `interleaved_quantization.md` |
| P7 | Between-class margin < 1 ULP is **necessary for impurity with zero exceptions** (all 15 impure rows have margin < 1 ULP; 0 of 1,650 rows at ≥ 1 step are impure) but **not sufficient** (40 of 55 sub-ULP rows stay pure); exact cell coincidence predicts impurity exactly. *Audit correction 2026-08-25: the previous row read "margin < 1 ULP does not predict the 15 impure rows" — the opposite of the source document's (correct) finding; the compression inverted necessity-without-sufficiency into non-prediction.* | current | margin analysis, Chebyshev/local-ULP | measurement | — | `between_class_margin.md`, `AUDIT.md` |

## Count-language corrections (audit of 2026-08-22)

Earlier documents pooled the monotonic-zero count inconsistently: "3,330"
(corrugation + parametrization only), "4,610" (+ threshold), "4,970"
(+ new restarts) — all silently excluding the width-sweep (240) and
protocol-sweep (360) monotonic strata. The artifact-derived total is
**5,570**, verified by recomputation from the recorded frames. Correction
notes have been added where the older totals appear; the older totals were
each true of their stated subsets, mislabeled as totals.

**Audit reconciliation (2026-08-24).** The "5,570" above is itself a stale
snapshot: it is the pre-deduplication, pre-winding sum
(240+1,280+1,440+1,890+360+360) with the restarts stratum still miscounted
as 360. The artifact-derived distinct total is **5,580**
(240+1,280+1,440+1,620+360+400+240), verified row-by-row in `AUDIT.md`
Part 1. Chain of totals for the record: 3,330 → 4,610 → 4,970 (subset
pools, mislabeled) → 5,570 (full pool, undeduplicated, restarts
miscounted) → 5,540 (deduplicated, winding added, restarts still
miscounted) → **5,580 (current)**. Every version of the count has been a
zero; only the denominator has moved, each time upward.
